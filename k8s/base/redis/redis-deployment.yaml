# Redis Deployment
# Used for distributed caching, Pub/Sub, and task queue (ARQ)
# Enables horizontal scaling of backend API pods

apiVersion: apps/v1
kind: Deployment
metadata:
  name: redis
  namespace: tesslate
  labels:
    app: redis
    component: cache
spec:
  revisionHistoryLimit: 3
  replicas: 1
  selector:
    matchLabels:
      app: redis
      component: cache
  template:
    metadata:
      labels:
        app: redis
        component: cache
    spec:
      containers:
      - name: redis
        image: redis:7-alpine
        ports:
        - containerPort: 6379
          name: redis
        command:
          - redis-server
          - --maxmemory
          - "512mb"
          - --maxmemory-policy
          - volatile-lru
          - --appendonly
          - "yes"
        volumeMounts:
          - name: redis-data
            mountPath: /data
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "768Mi"
            cpu: "500m"
        livenessProbe:
          exec:
            command:
              - redis-cli
              - ping
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          exec:
            command:
              - redis-cli
              - ping
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
      volumes:
        - name: redis-data
          persistentVolumeClaim:
            claimName: redis-data
