# =============================================================================
# DigitalOcean Container Registry Credentials
# =============================================================================
# This file is a TEMPLATE. Do NOT commit real credentials.
#
# To create this secret:
# 1. Install doctl: brew install doctl
# 2. Login: doctl auth init
# 3. Create secret: doctl kubernetes cluster kubeconfig save <cluster-name>
# 4. Run: kubectl create secret docker-registry tesslate-container-registry-nyc3 \
#         --docker-server=registry.digitalocean.com \
#         --docker-username=<your-token> \
#         --docker-password=<your-token> \
#         -n tesslate
#
# Or use the setup script:
#   cd k8s/scripts/deployment && ./setup-registry-auth.sh
# =============================================================================

apiVersion: v1
kind: Secret
metadata:
  name: tesslate-container-registry-nyc3
  namespace: tesslate
type: kubernetes.io/dockerconfigjson
data:
  # Base64 encoded docker config
  # Generate with: doctl registry kubernetes-manifest | base64
  .dockerconfigjson: REPLACE_WITH_BASE64_ENCODED_DOCKER_CONFIG
