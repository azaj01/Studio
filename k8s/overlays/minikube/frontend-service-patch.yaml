# Frontend Service Patch for Minikube
# Vite dev server runs on 5173

apiVersion: v1
kind: Service
metadata:
  name: tesslate-frontend-service
  namespace: tesslate
spec:
  ports:
  - port: 80
    targetPort: 5173
    protocol: TCP
    name: http
